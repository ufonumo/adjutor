import Tables from "../../../../components/Table/tables";

const Pricing = () => {
    return (
        <div>
            <Tables header="Endpoint URL Pricing" dataSource={[]} columns={[]} />
        </div>
    );
};

export default Pricing;
