import Tables from "../../../../components/Table/tables";

const Team = () => {
    return (
        <div>
            <Tables header="Role" dataSource={[]} columns={[]} />
        </div>
    );
};

export default Team;
