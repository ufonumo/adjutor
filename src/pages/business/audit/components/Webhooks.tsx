import Tables from "../../../../components/Table/tables";

const Webhooks = () => {
    return (
        <div>
            <Tables header="Webhooks Logs" dataSource={[]} columns={[]} />
        </div>
    );
};

export default Webhooks;
