import Tables from "../../../../components/Table/tables";

const ApiLog = () => {
    return (
        <div>
            <Tables header="API Logs" dataSource={[]} columns={[]} />
        </div>
    );
};

export default ApiLog;
