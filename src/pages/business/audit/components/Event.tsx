import Tables from "../../../../components/Table/tables";

const EventAudit = () => {
    return (
        <div>
            <Tables header="Events" dataSource={[]} columns={[]} />
        </div>
    );
};

export default EventAudit;
